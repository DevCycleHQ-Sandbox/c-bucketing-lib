name: Build and Test
on:
  push:
env:
  BUILD_TYPE: Release
jobs:
  build:
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-12 ]
    runs-on: ${{ matrix.os }}
    steps:
       - id: Build and Test
         uses: ./.github/workflows/cmake-composite@main
